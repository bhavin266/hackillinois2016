<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".UpdateInterestActivity"
    tools:showIn="@layout/activity_update_interest">
    <MultiAutoCompleteTextView
        android:id="@+id/searchSubjects"
        android:layout_below="@id/textView"
        android:layout_marginBottom="8dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:hint="Search and Add"
        android:padding="25dp" />

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/buttonAddInterests"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/searchSubjects"
        android:layout_marginBottom="4dp"
        android:layout_marginTop="24dp"
        android:padding="12dp"
        android:text="Done!"
        android:onClick="addInterests" />

    <Button
        android:id="@+id/back"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonAddInterests"
        android:layout_marginBottom="24dp"
        android:layout_marginTop="2dp"
        android:padding="12dp"
        android:text="Back"
        android:onClick="goback" />

</RelativeLayout>
